# Logging -
# It means you can capture details which are useful while debugging and show the user details about execution of program
#  Warning to users
# Information to the users
# Errors. Critical errors user.


# Import logging
# then create pytest.ini file
# It should be in root folder (parent folder)
# LOGGER = logging.getLogger(_name_)
# in code LOGGER.info(driver.title)
# pytest "file path or name" -s -v

# -v - verbose - Increase the logs
# -s to print the print logs
# -q (quite the logs - few logs)
# -k Expressions

# pytest.ini command
# [pytest]
# log_cli
# log_cli_level = INFO
# log_cli_format = %(asctime)s [%(levelname)8s] %(message)s (%(filename)s:%(lineno)s)
# log_cli_date_format=%Y-%m-%d %H:%M:%S

# Interview Question
# Close - Close will close the current window only (it will not close the other tabs)
  # Session != null

# Quit - It will close complete browser (close all the tabs or windows)
  # Session == None

# Chrome=> ChromeOptions
#Chrome Options - Chrome with extension, window size, proxy, JS disabled





